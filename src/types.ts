export interface Project {
    title: string;
    description: string;
    imageUrl: string;
    liveLink: string;
    repoLink: string;
    skills: string[];
    videoUrl?: string; // Optional video URL for YouTube videos
  }